<?xml version="1.0" encoding="utf-8"?>
<!--
  The ink slab IS the icon, so it is the background layer and the launcher's
  mask cuts it. Drawn as a square inside the foreground it read as a square
  floating in a circle, with a ring of paper around it that no other icon on
  the home screen has.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#14161C" />
</shape>
